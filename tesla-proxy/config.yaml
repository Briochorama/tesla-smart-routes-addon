name: "Tesla HTTP Proxy"
description: "Tesla VCP proxy — signs navigation commands for Tesla Fleet API"
version: "1.0.0"
slug: "tesla_proxy"
init: false
arch:
  - aarch64
  - amd64
map:
  - config:ro
  - data:rw
ports:
  4443/tcp: 4443
